/// Spec: specs/POLYGIT_CE_APP_GRAPH_SPEC.md
/// PolyGit App Graph — defines 13 ModuleNode declarations for PolyGit,
/// registers intra-graph dependency edges and cross-graph bridges to es-vcs,
/// QKit CI, and PolyDocs.
///
/// Epic: qgit#TBD (CE + App Graph Integration)
/// govern lex polyqlabs/qgit

lex polyqlabs/qgit

@target software
@codegen_targets ["wasm", "rust"]

import "core/deployment/module_stratum.fl" {
    ModuleNode, ModuleEdge, SemVer, CompilationReceipt, ResourceBudget8D,
    CsrStorage, ModuleType, AperturePartition, ModuleState, RolloutState,
    PersistencePolicy, OfflineCachePolicy, EntitlementTier, EnforcementMode,
    SLATier, EDGE_REQUIRES, EDGE_BRIDGE_TO,
    module_graph_add_module, module_graph_add_edge,
    graph_add_node, graph_add_edge,
}

import "core/deployment/cross_graph_edges.fl" {
    EDGE_GOVERNANCE_OBSERVE, EDGE_COMPILED_BY,
    GovernanceObserveEdge, CompilationProvenanceEdge,
    module_graph_add_governance_observe_edge,
    module_graph_add_compilation_provenance_edge,
}

// ── Constants ──────────────────────────────────────────────────────────────────

const POLYGIT_VERSION_MAJOR: u32 = 0
const POLYGIT_VERSION_MINOR: u32 = 2
const POLYGIT_VERSION_PATCH: u32 = 0
const POLYGIT_APP_LEX: string = "polyqlabs/qgit"
const POLYGIT_MODULE_COUNT: u32 = 13

// ═══════════════════════════════════════════════════════════════════════════════
// Internal helper — common ModuleNode construction
// ═══════════════════════════════════════════════════════════════════════════════

circuit make_qgit_module(
    name: string, partition: u8, sla: u8, budget: ResourceBudget8D
) -> ModuleNode
    lex polyqlabs/qgit
    @status production
    @precision D
{
    let ver = SemVer { major: POLYGIT_VERSION_MAJOR, minor: POLYGIT_VERSION_MINOR,
        patch: POLYGIT_VERSION_PATCH, pre_release: "" }
    let receipt = CompilationReceipt {
        source_hash: bytes(32, 0), compiler_version: ver,
        output_hash: bytes(32, 0), prime_device_id: Option::None,
        compilation_timestamp: 0, reproducible_verified: false,
        receipt_signature: bytes(4627, 0),
    }

    ModuleNode {
        name: name,
        version_major: POLYGIT_VERSION_MAJOR, version_minor: POLYGIT_VERSION_MINOR,
        version_patch: POLYGIT_VERSION_PATCH, version_pre: "",
        module_type: ModuleType::SmartCircuit as u8,
        content_hash: bytes(32, 0), signature_chain: bytes(0),
        aperture_partition: partition,
        role_set: ["Developer", "Admin"],
        required_entitlement: EntitlementTier::Pro as u8,
        classification_domains: [], max_classification_level: 0,
        compilation_receipt: receipt,
        source_hash: bytes(32, 0),
        compiler_version_major: POLYGIT_VERSION_MAJOR,
        compiler_version_minor: POLYGIT_VERSION_MINOR,
        prime_device_id: bytes(0), reproducible_verified: false,
        resource_budget: budget,
        budget_enforcement_mode: EnforcementMode::Hard as u8,
        state: ModuleState::Active as u8,
        rollout_state: RolloutState::Canary as u8,
        canary_target: Option::None,
        persistence: PersistencePolicy::Persistent as u8,
        offline_cache_policy: OfflineCachePolicy::CacheAndVerify as u8,
        max_staleness_seconds: 3600, sla_tier: sla,
        preferred_locality: "",
        created_at_ms: 0, updated_at_ms: 0,
    }
}

// ═══════════════════════════════════════════════════════════════════════════════
// §1  Module Definitions (13 modules)
// ═══════════════════════════════════════════════════════════════════════════════

circuit qgit_branch_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_branch", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 6000, hash_ops: 200, bandwidth: 2000,
            storage: 1000, observe_events: 200, proofs: 10, consensus: 5, memory: 4096 })
}

circuit qgit_ci_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_ci", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 10000, hash_ops: 300, bandwidth: 5000,
            storage: 2000, observe_events: 500, proofs: 10, consensus: 5, memory: 8192 })
}

circuit qgit_commit_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_commit", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 8000, hash_ops: 500, bandwidth: 3000,
            storage: 2000, observe_events: 300, proofs: 20, consensus: 10, memory: 8192 })
}

circuit qgit_document_review_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_document_review", AperturePartition::Backend as u8,
        SLATier::Standard as u8,
        ResourceBudget8D { execution: 5000, hash_ops: 100, bandwidth: 2000,
            storage: 1000, observe_events: 200, proofs: 5, consensus: 2, memory: 4096 })
}

circuit qgit_governance_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_governance", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 8000, hash_ops: 200, bandwidth: 2000,
            storage: 1000, observe_events: 300, proofs: 15, consensus: 10, memory: 4096 })
}

circuit qgit_issue_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_issue", AperturePartition::Backend as u8,
        SLATier::Standard as u8,
        ResourceBudget8D { execution: 5000, hash_ops: 100, bandwidth: 2000,
            storage: 1500, observe_events: 200, proofs: 5, consensus: 2, memory: 4096 })
}

circuit qgit_lfs_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_lfs", AperturePartition::Backend as u8,
        SLATier::Standard as u8,
        ResourceBudget8D { execution: 6000, hash_ops: 300, bandwidth: 10000,
            storage: 5000, observe_events: 100, proofs: 5, consensus: 2, memory: 8192 })
}

circuit qgit_metering_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_metering", AperturePartition::Shared as u8,
        SLATier::Standard as u8,
        ResourceBudget8D { execution: 3000, hash_ops: 100, bandwidth: 1000,
            storage: 500, observe_events: 200, proofs: 5, consensus: 2, memory: 2048 })
}

circuit qgit_platform_health_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_platform_health", AperturePartition::Shared as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 3000, hash_ops: 50, bandwidth: 500,
            storage: 300, observe_events: 500, proofs: 5, consensus: 2, memory: 2048 })
}

circuit qgit_rbac_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_rbac", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 5000, hash_ops: 200, bandwidth: 1000,
            storage: 500, observe_events: 200, proofs: 10, consensus: 5, memory: 4096 })
}

circuit qgit_repository_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_repository", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 8000, hash_ops: 300, bandwidth: 3000,
            storage: 3000, observe_events: 300, proofs: 10, consensus: 5, memory: 8192 })
}

circuit qgit_review_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_review", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 7000, hash_ops: 200, bandwidth: 3000,
            storage: 1500, observe_events: 300, proofs: 10, consensus: 5, memory: 4096 })
}

circuit qgit_repo_graph_module() -> ModuleNode
    lex polyqlabs/qgit  @status production  @precision D
    observe metrics: [module_name]  meters [compute_cycles]
{
    make_qgit_module("qgit_repo_graph", AperturePartition::Backend as u8,
        SLATier::Premium as u8,
        ResourceBudget8D { execution: 10000, hash_ops: 500, bandwidth: 5000,
            storage: 5000, observe_events: 500, proofs: 20, consensus: 10, memory: 16384 })
}

// ═══════════════════════════════════════════════════════════════════════════════
// §2  Graph Registration (13 modules, EDGE_REQUIRES)
// ═══════════════════════════════════════════════════════════════════════════════

circuit qgit_app_graph_register(graph: CsrStorage) -> CsrStorage
    lex polyqlabs/qgit
    @status production
    @precision B
    @attested true
    observe metrics: [qgit_module_count, qgit_edge_count, register_latency_ns]
    meters [compute_cycles, memory_bytes]
{
    let (g, id_branch) = module_graph_add_module(graph, qgit_branch_module())
    let (g, id_ci) = module_graph_add_module(g, qgit_ci_module())
    let (g, id_commit) = module_graph_add_module(g, qgit_commit_module())
    let (g, id_doc_review) = module_graph_add_module(g, qgit_document_review_module())
    let (g, id_governance) = module_graph_add_module(g, qgit_governance_module())
    let (g, id_issue) = module_graph_add_module(g, qgit_issue_module())
    let (g, id_lfs) = module_graph_add_module(g, qgit_lfs_module())
    let (g, id_metering) = module_graph_add_module(g, qgit_metering_module())
    let (g, id_health) = module_graph_add_module(g, qgit_platform_health_module())
    let (g, id_rbac) = module_graph_add_module(g, qgit_rbac_module())
    let (g, id_repository) = module_graph_add_module(g, qgit_repository_module())
    let (g, id_review) = module_graph_add_module(g, qgit_review_module())
    let (g, id_repo_graph) = module_graph_add_module(g, qgit_repo_graph_module())

    let ver = "0.2.0"
    let vc = ">=0.2.0"

    // qgit_branch -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_branch", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_branch, id_repository)

    // qgit_branch -> qgit_commit
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_branch", source_version: ver,
        target_module: "qgit_commit", target_version_constraint: vc, metadata: bytes(0),
    }, id_branch, id_commit)

    // qgit_ci -> qgit_commit
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_ci", source_version: ver,
        target_module: "qgit_commit", target_version_constraint: vc, metadata: bytes(0),
    }, id_ci, id_commit)

    // qgit_ci -> qgit_branch
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_ci", source_version: ver,
        target_module: "qgit_branch", target_version_constraint: vc, metadata: bytes(0),
    }, id_ci, id_branch)

    // qgit_commit -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_commit", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_commit, id_repository)

    // qgit_document_review -> qgit_review
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_document_review", source_version: ver,
        target_module: "qgit_review", target_version_constraint: vc, metadata: bytes(0),
    }, id_doc_review, id_review)

    // qgit_governance -> qgit_rbac
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_governance", source_version: ver,
        target_module: "qgit_rbac", target_version_constraint: vc, metadata: bytes(0),
    }, id_governance, id_rbac)

    // qgit_governance -> qgit_branch
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_governance", source_version: ver,
        target_module: "qgit_branch", target_version_constraint: vc, metadata: bytes(0),
    }, id_governance, id_branch)

    // qgit_issue -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_issue", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_issue, id_repository)

    // qgit_lfs -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_lfs", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_lfs, id_repository)

    // qgit_lfs -> qgit_commit
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_lfs", source_version: ver,
        target_module: "qgit_commit", target_version_constraint: vc, metadata: bytes(0),
    }, id_lfs, id_commit)

    // qgit_review -> qgit_commit
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_review", source_version: ver,
        target_module: "qgit_commit", target_version_constraint: vc, metadata: bytes(0),
    }, id_review, id_commit)

    // qgit_review -> qgit_branch
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_review", source_version: ver,
        target_module: "qgit_branch", target_version_constraint: vc, metadata: bytes(0),
    }, id_review, id_branch)

    // qgit_review -> qgit_rbac
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_review", source_version: ver,
        target_module: "qgit_rbac", target_version_constraint: vc, metadata: bytes(0),
    }, id_review, id_rbac)

    // qgit_repo_graph -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_repo_graph", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_repo_graph, id_repository)

    // qgit_repo_graph -> qgit_commit
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_repo_graph", source_version: ver,
        target_module: "qgit_commit", target_version_constraint: vc, metadata: bytes(0),
    }, id_repo_graph, id_commit)

    // qgit_metering -> qgit_repository
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_metering", source_version: ver,
        target_module: "qgit_repository", target_version_constraint: vc, metadata: bytes(0),
    }, id_metering, id_repository)

    // qgit_platform_health -> qgit_metering
    let g = module_graph_add_edge(g, ModuleEdge {
        edge_type: EDGE_REQUIRES, source_module: "qgit_platform_health", source_version: ver,
        target_module: "qgit_metering", target_version_constraint: vc, metadata: bytes(0),
    }, id_health, id_metering)

    g
}

// ═══════════════════════════════════════════════════════════════════════════════
// §3  Cross-Graph Bridge Edges
// ═══════════════════════════════════════════════════════════════════════════════

/// Bridge edges: PolyGit -> es-vcs (commit wire protocol), QKit CI (pipeline
/// dispatch), PolyDocs (document review integration).
circuit qgit_register_bridge_edges(graph: CsrStorage) -> CsrStorage
    lex polyqlabs/qgit
    @status production
    @precision B
    @attested true
    observe metrics: [bridge_edge_count]
    meters [compute_cycles]
{
    let id_commit = find_qgit_module_id(graph, "qgit_commit")
    let id_ci = find_qgit_module_id(graph, "qgit_ci")
    let id_doc_review = find_qgit_module_id(graph, "qgit_document_review")

    // BRIDGE_TO: qgit_commit -> es-vcs es_vcs_core (commit wire protocol)
    let bridge_vcs = serialize(BridgeEdgeData {
        source_lex: POLYGIT_APP_LEX, source_module: "qgit_commit",
        target_lex: "core/vcs", target_module: "es_vcs_core",
        bridge_type: "commit_wire_protocol", timestamp_ms: 0,
    }) as bytes
    let (g, bid1) = graph_add_node(graph, 0xB0, bridge_vcs)
    let g = graph_add_edge(g, id_commit, bid1, EDGE_BRIDGE_TO, bridge_vcs)

    // BRIDGE_TO: qgit_ci -> QKit CI qkit_ci_runner (pipeline dispatch)
    let bridge_ci = serialize(BridgeEdgeData {
        source_lex: POLYGIT_APP_LEX, source_module: "qgit_ci",
        target_lex: "polyqlabs/qkit/ci", target_module: "qkit_ci_runner",
        bridge_type: "pipeline_dispatch", timestamp_ms: 0,
    }) as bytes
    let (g, bid2) = graph_add_node(g, 0xB0, bridge_ci)
    let g = graph_add_edge(g, id_ci, bid2, EDGE_BRIDGE_TO, bridge_ci)

    // BRIDGE_TO: qgit_document_review -> PolyDocs qdocs_review (doc review)
    let bridge_docs = serialize(BridgeEdgeData {
        source_lex: POLYGIT_APP_LEX, source_module: "qgit_document_review",
        target_lex: "polyqlabs/qdocs", target_module: "qdocs_review",
        bridge_type: "document_review", timestamp_ms: 0,
    }) as bytes
    let (g, bid3) = graph_add_node(g, 0xB0, bridge_docs)
    let g = graph_add_edge(g, id_doc_review, bid3, EDGE_BRIDGE_TO, bridge_docs)

    g
}

// ═══════════════════════════════════════════════════════════════════════════════
// §4  Governance Edges
// ═══════════════════════════════════════════════════════════════════════════════

/// Register GOVERNANCE_OBSERVE edges for all 13 PolyGit modules.
circuit qgit_register_governance_edges(
    graph: CsrStorage,
    governance_lex: string
) -> CsrStorage
    lex polyqlabs/qgit
    @status production
    @precision B
    @attested true
    observe metrics: [governance_edge_count]
    meters [compute_cycles]
{
    let modules = [
        "qgit_branch", "qgit_ci", "qgit_commit",
        "qgit_document_review", "qgit_governance", "qgit_issue",
        "qgit_lfs", "qgit_metering", "qgit_platform_health",
        "qgit_rbac", "qgit_repository", "qgit_review",
        "qgit_repo_graph",
    ]

    let g = graph
    for name in modules {
        let module_id = find_qgit_module_id(g, name)
        g = module_graph_add_governance_observe_edge(g, GovernanceObserveEdge {
            observer_lex: governance_lex, observer_module: "governance_core",
            observed_lex: POLYGIT_APP_LEX, observed_module: name,
            observe_scope: "full", read_only: true,
            witness_chain_required: false, refresh_interval_ms: 60000,
        }, module_id)
    }
    g
}

// ═══════════════════════════════════════════════════════════════════════════════
// Internal helpers
// ═══════════════════════════════════════════════════════════════════════════════

data BridgeEdgeData : polyqlabs/qgit/bridge v1 {
    source_lex: string,
    source_module: string,
    target_lex: string,
    target_module: string,
    bridge_type: string,
    timestamp_ms: u64,
}

circuit find_qgit_module_id(graph: CsrStorage, name: string) -> u32
    lex polyqlabs/qgit
    @status production
    @precision D
{
    for i in 0..graph.num_nodes {
        let node_data = graph.node_data[i]
        let module = deserialize(node_data) as ModuleNode
        if module.name == name {
            return i
        }
    }
    0xFFFFFFFF
}

// ── Metrics Store ──────────────────────────────────────────────────────────────

store series qgit_app_graph_metrics
    lex polyqlabs/qgit/metrics
{
    operation: string key
    module_count: u32
    edge_count: u32
    register_latency_ns: u64
    bridge_edge_count: u32
    governance_edge_count: u32
}

// ── Golden Tests ───────────────────────────────────────────────────────────────

#[test]
circuit test_qgit_app_graph_register()
    lex polyqlabs/qgit  @status test
{
    let graph = CsrStorage::empty()
    let result = qgit_app_graph_register(graph)
    assert result.num_nodes == POLYGIT_MODULE_COUNT
    assert result.num_edges >= 18
}

#[test]
circuit test_qgit_bridge_edges()
    lex polyqlabs/qgit  @status test
{
    let graph = CsrStorage::empty()
    let graph = qgit_app_graph_register(graph)
    let result = qgit_register_bridge_edges(graph)
    let bridge_count = count_edges_of_type(result, EDGE_BRIDGE_TO)
    assert bridge_count == 3
}

#[test]
circuit test_qgit_governance_edges()
    lex polyqlabs/qgit  @status test
{
    let graph = CsrStorage::empty()
    let graph = qgit_app_graph_register(graph)
    let result = qgit_register_governance_edges(graph, "core/governance")
    let gov_count = count_edges_of_type(result, EDGE_GOVERNANCE_OBSERVE)
    assert gov_count == POLYGIT_MODULE_COUNT
}

#[test]
circuit test_qgit_module_lookup()
    lex polyqlabs/qgit  @status test
{
    let graph = CsrStorage::empty()
    let graph = qgit_app_graph_register(graph)
    let id = find_qgit_module_id(graph, "qgit_commit")
    assert id != 0xFFFFFFFF
    let missing = find_qgit_module_id(graph, "nonexistent")
    assert missing == 0xFFFFFFFF
}
